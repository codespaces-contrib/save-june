{
    "name": "Haikus for june (Worker)",
    "workspaceFolder": "/workspace/worker",
    "dockerComposeFile": [
        "../docker-compose.yml",
        "../docker-compose.devcontainer.yml"
    ],
    "service": "worker",
    "runServices": ["worker"],
    "remoteUser": "worker",
    "settings": {
        "go.toolsManagement.checkForUpdates": "local",
        "go.useLanguageServer": true,
        "go.gopath": "/go",
        "go.goroot": "/usr/local/go"
    },
    "extensions": [
        "golang.Go"
    ],
    "portsAttributes": {
        "8080": {
            "label": "Worker",
            "onAutoForward": "notify"
        }
    },
    "postCreateCommand": "go get"
    // 👇 Needs to be placed in docker-compose.yml ☹️
    // "runArgs": [
    //    "--cap-add=SYS_PTRACE",
    //    "--security-opt",
    //    "seccomp=unconfined"
    //]
}